import { DecimalOctalPipe } from './decimal-octal.pipe';

describe('DecimalOctalPipe', () => {
  it('create an instance', () => {
    const pipe = new DecimalOctalPipe();
    expect(pipe).toBeTruthy();
  });
});
