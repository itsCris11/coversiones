import { DecimalBinarioPipe } from './decimal-binario.pipe';

describe('DecimalBinarioPipe', () => {
  it('create an instance', () => {
    const pipe = new DecimalBinarioPipe();
    expect(pipe).toBeTruthy();
  });
});
